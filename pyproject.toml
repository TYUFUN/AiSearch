[project]
name = "ai_browser_cli"
version = "0.1.0"
description = "CLI tool for cli browser with AI"
requires-python = ">=3.11"
dependencies = [
    "openai>=3.16.2",
    "python-dotenv>=1.2.3",
    "typer>=0.27.2",
]
[dependency-groups]
dev = [
    "pyinstaller>=6.0.0",
]
