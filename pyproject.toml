[project]
name = "my-project"
version = "0.1.0"
description = "CLI/скрипт с OpenAI API"
requires-python = ">=3.11"
dependencies = [
    "openai>=3.16.2",
    "python-dotenv>=1.2.3",
    "typer>=0.27.2",
]
[tool.uv]
dev-dependencies = [
    "pyinstaller>=6.0.0",
]
